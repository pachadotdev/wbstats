% Generated by tinyroxygen: do not edit by hand
% Please edit documentation in wb_data_coverage.R
\name{wb_data_coverage}
\alias{wb_data_coverage}
\title{Summarize Data Coverage for World Bank Indicators}
\usage{
wb_data_coverage(data, by = c("iso2c", "iso3c", "country"))
}

\arguments{
\item{data}{A \code{data.table}/\code{data.frame} as returned by \code{\link[=wb_data]{wb_data()}}, in either wide format (default) or long format (\code{return_wide = FALSE})}

\item{by}{Character vector of column names in \code{data} to group by. Default is \code{c("iso2c", "iso3c", "country")}, which reports, for each location, the \% of dates with non-missing data for each indicator. Pass \code{"date"} instead to report, for each date, the \% of locations with non-missing data for each indicator}
}

\value{
A \code{data.table} with one row per group in \code{by}, and one column per indicator giving the percentage (0-100) of non-missing values within that group
}

\description{
Given a \code{data.table}/\code{data.frame} as returned by \code{\link[=wb_data]{wb_data()}}, this function reports, for each group defined by \code{by}, the percentage of non-missing (non-\code{NA}) observations available per indicator. This is useful for quickly comparing data coverage across countries or over time for a set of indicators, without having to download each indicator separately and calculate coverage manually.
}

\examples{
\dontrun{
my_indicators <- c(
  life_exp   = "SP.DYN.LE00.IN",
  gdp_capita = "NY.GDP.PCAP.CD",
  pop        = "SP.POP.TOTL"
)

d <- wb_data(my_indicators, start_date = 2000, end_date = 2020)

# \\% of years (2000-2020) with data, per country, per indicator
wb_data_coverage(d)

# \\% of countries with data, per year, per indicator
wb_data_coverage(d, by = "date")
}
}

